// Package tools provides build-time version information
package tools

var (
	// Version is the build version, set at build time
	Version = "unknown version"
	// BuildTime is the build time, set at build time
	BuildTime = "unknown time"
)
